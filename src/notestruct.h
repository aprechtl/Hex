#ifndef NOTESTRUCT_H
#define NOTESTRUCT_H

struct NoteStruct
{
    int track;
    double startPosition, duration;
    unsigned char velocity;
    short int j, k;
};

#endif // NOTESTRUCT_H
